package com.globant.controller;

/**
 * The {@code UserController} interface defines the method "Run".
 * Every logged-in controller implements this interface to easily be implemented with the facade practice.
 */
public interface UserController {
    void run();
}